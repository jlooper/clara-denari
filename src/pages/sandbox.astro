---
import Layout from '../layouts/Layout.astro';
import CloudinarySandbox from '../components/CloudinarySandbox.vue';

const title = 'Cloudinary Sandbox';
const backdrop = '';
---

<Layout title={title} backdrop={backdrop}>
  <div class="prose max-w-none">
    <h1>{title}</h1>
    <p class="mb-6">
      Test Cloudinary image transformations in real-time. Enter your cloud name, public ID, and transformation parameters to see the results.
    </p>
    
    <CloudinarySandbox client:load />
    
    <div class="mt-8 p-4 bg-black border border-green-500 rounded">
      <h3 class="text-green-500 text-lg font-bold mb-3">Usage Tips:</h3>
      <ul class="text-green-500 text-sm space-y-2">
        <li>• Use "demo" as cloud name and "sample" as public ID for testing</li>
        <li>• Try the preset buttons for common transformations</li>
        <li>• Transformation parameters follow Cloudinary's URL API format</li>
        <li>• Examples: <code class="bg-gray-800 px-1 rounded">c_scale,w_300,h_200</code> or <code class="bg-gray-800 px-1 rounded">e_grayscale,c_fill,w_400,h_300</code></li>
        <li>• The generated URL can be copied and used directly</li>
      </ul>
    </div>
  </div>
</Layout> 